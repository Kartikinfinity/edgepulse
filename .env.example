# ---------------------------------------------------------------------------
# SIH KWS - machine-specific settings.
#
#   cp .env.example .env      (Linux/macOS)
#   Copy-Item .env.example .env   (Windows PowerShell)
#
# .env is git-ignored. NEVER commit real credentials.
# Every value below is OPTIONAL - the defaults are relative to the repo root
# and work unchanged on a fresh clone.
# Precedence: real environment variable > .env > built-in default.
# ---------------------------------------------------------------------------

# --- Paths -----------------------------------------------------------------
# NOTE: there is NO approved dataset yet. The previous corpus was deprecated
# (see DATASET.md). These point at where the NEW dataset and its source
# recordings will be built.
#
# Where the built/curated dataset lives.  Default: <repo>/data/dataset
# Relative values resolve against the repo root; absolute values are honoured.
#SIH_DATASET_ROOT=data/dataset

# Where raw source recordings live before curation. Default: <repo>/data/recordings
# KEEP THE RAW RECORDINGS. The previous project could not re-cut its clips
# because the raw sessions were lost - do not repeat that.
#SIH_RECORDINGS_ROOT=data/recordings

# Features, checkpoints, exported models. Default: <repo>/artifacts
#SIH_ARTIFACTS_DIR=artifacts

# OPTIONAL external corpus (e.g. Google Speech Commands) as candidate negative
# or background material for the new dataset. Not required by any phase.
#SIH_SPEECH_COMMANDS_ROOT=/path/to/speech_commands

# --- Hardware --------------------------------------------------------------
# Serial port of the ESP32-S3. Leave unset to let PlatformIO auto-detect.
# Windows example: COM7        Linux example: /dev/ttyACM0
# NOTE: the port number differs per machine. Do not assume COM7.
#SIH_UPLOAD_PORT=

#SIH_MONITOR_BAUD=115200

# --- Server / demo ---------------------------------------------------------
#SIH_ASR_SERVER_HOST=0.0.0.0
#SIH_ASR_SERVER_PORT=8765
#SIH_DASHBOARD_PORT=8080

# --- SECRETS ---------------------------------------------------------------
# Wi-Fi credentials for the ESP32. REQUIRED before Phase G3.
# These are read at firmware build time and MUST NOT be committed.
# Leave blank here; fill them in your local .env only.
#SIH_WIFI_SSID=
#SIH_WIFI_PASSWORD=
